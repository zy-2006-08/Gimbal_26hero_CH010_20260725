# 生成保姆级图解版开发环境文档（完整独立版）

## TL;DR (For humans)

在 `D:\Newcode\` 新建一份**完整独立、保姆级、图解**的现代嵌入式开发环境教程（面向已会 Keil+串口、转现代工具链的 RM 战队队员，纯净电脑起步，全覆盖到调试+多工程+clangd）。区别于原速查手册 `新手配置指南.md`（那份是速查风格，不动它）。

**文件名**：`D:\Newcode\RM战队嵌入式开发环境圣经-保姆级图解.md`
**风格**：Markdown，充分用标题分层/加粗/代码高亮/表格/emoji/Mermaid流程图/截图占位。用户最终用 Typora 补截图后导出 PDF 发布。
**配置处理**：方案 A —— 配置代码照贴，每段前用大白话说"这段干嘛"，关键行加行内注释。
**图**：Mermaid 流程图 + ASCII 图我来写；真实截图用 `【截图占位:...】` 标记，用户后续补。

## 关键约束（所有章节共用）
- 目标读者：会 Keil+串口调试，**不会** PATH 配置、PowerShell、CMake/Ninja/工具链概念。
- 纯净电脑起步（含装 VSCode、开 PowerShell 等最基础步骤）。
- 通俗精简的大白话，但完整自包含（读者只看这一份就能从零搭好）。
- 配置真实内容参照原 `新手配置指南.md`（逐字准确），但要加白话解释+注释。
- 截图占位格式统一：`> 📷 **【截图占位】** 描述要截什么`，步骤可拆细多给占位。
- Mermaid 代码块用 ```mermaid 围栏。
- 每章开头一句"这一章你会得到什么"。

## 标题（写在文档最顶部）
```
# RM 战队嵌入式开发环境圣经 · 保姆级图解｜脱离 Keil，拥抱现代工具链

### CMake+Ninja+GCC 极速编译 · clangd 智能提示 · 双烧录器自动切换 · Ozone 高速调试 · 多工程一键流

> 面向已经在用 Keil + 串口调试、想转到现代工具链的你。纯净电脑起步，手把手图解，看完这一份就能从零搭好整套环境。
> 阅读/发布建议：用 Typora 打开本文 → 补入截图 → 导出 PDF，代码高亮、流程图、排版一应俱全。
```

## Todos（按章节分批写，每章一个任务，顺序执行避免同文件冲突）

- [x] **C0** 创建文件并写入：标题区 + 第0章《读前必读》（这份给谁看、你需要会什么、假设你不会什么、怎么用本文档=Typora补图导PDF、全书路线图用一个 Mermaid 流程图展示13章串起来的全景）。
- [x] **C1** 追加第1章《全景认知：这套东西到底是什么》——为什么脱离 Keil；Keil 是"全家桶一体机"，现代工具链是"积木自由组合"；一张 Mermaid 关系图（编辑器 VSCode / 编译器 gcc / 构建系统 CMake+Ninja / 烧录器 OpenOCD·J-Link / 调试器 Ozone·cortex-debug / 智能提示 clangd 各司其职、怎么串起来）；一句话建立心智模型。
- [x] **C2** 追加第2章《术语表》——6组词逐个：CMake/CMakeLists/Ninja/构建系统；工具链/gcc/arm-none-eabi；OpenOCD/J-Link/CMSIS-DAP 区别；compile_commands.json/clangd/LSP；ELF vs HEX/链接脚本.ld；多根工作区/${fileWorkspaceFolder}。每个=一句人话+Keil类比+为什么需要它。
- [x] **C3** 追加第3章《装软件（纯净电脑）》——含装 VSCode 本身；再逐个 gcc(arm-none-eabi)/CMake/Ninja/OpenOCD/J-Link驱动/Ozone：官网链接、选哪个包、装的时候勾什么。大量截图占位（每个软件下载页+安装关键步骤各留占位）。
- [x] **C4** 追加第4章《配 PATH 环境变量》——先用大白话讲"PATH 是什么"（电脑找程序的地址簿，Keil 帮你藏起来了，现在要自己配）；逐个工具加 PATH 的安全 PowerShell 写法（照抄原指南第2章，别用 setx 的坑）；截图占位（打开环境变量界面/验证版本号）；验证四条命令。
- [x] **C5** 追加第5章《PowerShell 基础》——只教这套流程要用的：怎么打开 PowerShell、cd 切目录、运行命令、ExecutionPolicy 是什么。截图占位。
- [x] **C6** 追加第6章《工程配置文件讲解》——CMakeLists.txt / cmake/gcc-arm-none-eabi.cmake / CMakePresets.json / openocd.cfg：每个文件先一段"这文件是干嘛的（Keil 里对应什么）"，再贴真实内容（参照原指南第3/4/5章逐字），关键行加 `# 白话注释`。方案A。
- [x] **C7** 追加第7章《一键快捷键》——tasks.json 三任务讲解 + keybindings（Alt+, / Alt+N / Alt+M）绑定步骤；截图占位（改键位设置界面）。
- [x] **C8** 追加第8章《第一次跑通：编译+烧录实操》——一个 Mermaid 流程图（改代码→Alt+,编译→接烧录器→Alt+N烧录→看现象）；分步截图占位；怎么看编译成功/失败输出。
- [x] **C9** 追加第9章《双烧录器自动切换》——大白话讲为什么要（DAP 免驱、J-Link 更快但驱动打架）；flash.ps1 作用（不用逐行讲脚本，讲它干嘛+怎么用）；插谁烧谁；截图占位。
- [x] **C10** 追加第10章《多工程一套快捷键》——单根 vs 多根工作区大白话解释（Mermaid 或 ASCII 图示意 ${fileWorkspaceFolder} 按当前文件分流）；.code-workspace 建法；两种打开方式都能用。
- [x] **C11** 追加第11章《clangd 智能提示》——装 clangd、关微软 IntelliSense（别卸载）、.clangd 文件配法；截图占位（扩展市场装 clangd、Disable IntelliSense 弹窗）；验证补全。
- [x] **C12** 追加第12章《Ozone 高速调试》——大白话：Ozone 只调试不编译、比 Keil 快在哪、必须先编译再加载 elf；新建工程连接步骤；看变量(Watched Data)、看波形(Data Sampling+Timeline)基本用法；电机安全提醒；截图占位。
- [x] **C13** 追加第13章《常见问题排错》——把原指南第10章避坑合集用大白话重写：找不到ninja、快捷键没反应、波浪线、烧录报错、HSS Error 84 等，每条"现象→原因→怎么修"。
- [x] **C14** 追加《附录 · 术语速查表》——把第2章术语浓缩成一张表（术语 | 一句话 | Keil类比），方便查阅；再加一句结语。
- [x] **C15** 最终校验：读回全文，确认标题、13章+附录齐全、所有 Mermaid/代码围栏闭合、截图占位格式统一、无残留 TODO。

## 用户后续（非本计划任务）
用 Typora 打开 → 在每个 `📷【截图占位】` 处补入实际截图 → 导出 PDF 发布。

## Must-NOT-Have
- 不改动原 `新手配置指南.md`（那份保留为速查版）。
- 不改任何工程配置文件、固件代码（本任务纯写新文档）。
- 不承诺生成真实截图（用占位标记，用户补）。
- 不做 Keil→现代工具链对照表（用户明确不要）。
- 配置代码必须与原指南真实内容一致，不臆造。
