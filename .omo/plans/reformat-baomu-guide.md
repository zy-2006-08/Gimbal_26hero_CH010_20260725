# 重新排版保姆级图解文档

## TL;DR (For humans)

重新排版 `D:\Newcode\RM战队嵌入式开发环境圣经-保姆级图解.md`（用户手动改过、插了截图的版本，761行）。核心：把所有指向系统临时文件夹的图片路径改成本地 `images/` 引用（保留原数字文件名），修复断裂的章节编号，统一格式。不改正文文字内容。

**用户后续**：把 `C:\Users\zy147\AppData\Local\Temp\` 里那些 png 复制到 `D:\Newcode\images\`（文件名不变），然后 Typora 导出 PDF。

## 关键约束
- **图片路径**：所有 `![数字](C:\Users\zy147\AppData\Local\Temp\1784XXXXXXXXX.png)` → 改成 `![有意义说明](images/1784XXXXXXXXX.png)`。**文件名（那串数字.png）必须原样保留**，只改前面的目录部分和方括号里的说明文字。
- **不改正文**：章节正文文字、代码块、表格内容一律不动。
- **只动**：图片引用行、章节编号、空行/间距、标题层级统一。
- 文件当前是 CRLF 换行（之前修过），保持 CRLF。

## 图片清单（逐个改，保留数字文件名）
以下每个 `C:\Users\zy147\AppData\Local\Temp\XXX.png` 都改为 `images/XXX.png`，方括号说明改成对应内容：
- 行52 `1784551438176.png` → 说明「arm-none-eabi-gcc 下载页」
- 行57 `1784551488165.png` → 「CMake 下载页」
- 行66 `1784551614506.png` → 「Ninja 下载页」
- 行74 `1784551769812.png` → 「OpenOCD 下载页」
- 行83 `1784551837378.png` → 「J-Link 驱动下载页」
- 行92 `1784551995799.png` → 「Ozone 下载页」
- 行115 `1784553151597.png` → 「四条命令验证版本号」
- 行129-133 `1784553230940 / 1784553244425 / 1784553258787 / 1784553265646 / 1784553301862.png` → 「图形化配置 PATH 步骤1~5」
- 行271 `1784553844364.png` → 「命令面板打开 keybindings.json」
- 行319 `1784553986194.png` → 「编译进度终端输出」
- 行404 `1784557843608.png` → 「设备管理器识别 J-Link」
- 行531 `1784558029895.png` → 「扩展市场安装 clangd」
- 行567-569 `1784558173500 / 1784558181797 / 1784558198116.png` → 「clangd 补全/跳转/报错验证」
- 行629-631 `1784558347459 / 1784558367310 / 1784558380739.png` → 「Ozone New Project 向导步骤」

## Todos

### 1. [x] 全局替换图片路径为 images/ 引用并加说明
**HOW**: 逐个 edit（或用脚本批量），把每个 `](C:\Users\zy147\AppData\Local\Temp\` 替换为 `](images/`，末尾 `.png)` 保留；同时把对应 `![数字]` 的方括号说明改成上面清单里的中文说明。文件名数字部分不变。
**EXPECT**: 所有图片引用变成 `![说明](images/1784XXXXX.png)` 形式，无一残留 `C:\Users\...\Temp\`。
**QA**: grep 确认文档里再无 `Temp\` 和 `C:\Users` 字样；grep `images/` 数量 = 原图片数量（约19张）。

### 2. [x] 修复章节编号连续性
**现状问题**：第1章直接从 1.2 开始（无1.1）；缺第0/2/5章标题；第9章 9.3→9.5（无9.4）；第12章 12.3→12.6（缺12.4/12.5）。
**HOW**: 只调整**编号数字**让其连续，不新增内容也不删内容。具体：
- 第1章：`### 1.2 各角色关系图` → `### 1.1 各角色关系图`（该章只剩这一节，改成1.1）。
- 第9章：`### 9.5 脚本放哪` → `### 9.4`；后续 9.6 → 9.5。
- 第12章：`### 12.6 为什么 Ozone 调试比 Keil 快` → `### 12.4`。
- 其余章节（3/4/6/7/8/10/11/13）编号本身连续，不动。
- 缺失的章号（第0/2/5章）是之前内容丢失造成，用户已明确不补内容，故**不补章、编号就从现有的算起**，不强行插空章。
**EXPECT**: 每章内部小节编号连续，无跳号。
**QA**: 读回确认无 x.2 却没 x.1、无跳号。

### 3. [x] 统一格式：图片与正文间距、空行
**HOW**: 每张图片引用前后各留一个空行（现在有些图紧贴文字，如行51-52、行629）；连续多张图（如 PATH 那5张、clangd 那3张）保持每行一张、整体前后留空行。标题与正文间空行统一。
**EXPECT**: 图文间距一致，Typora/PDF 里不挤。
**QA**: 抽查几处多图区域（行129-133、567-569、629-631）格式统一。

### 4. [x] 全文校验
读回全文：确认图片全 images/ 引用、编号连续、格式统一、CRLF 换行、正文文字未被改动、mermaid 代码块完好（4个）。

## 用户后续（非本计划任务）
1. 在 `D:\Newcode\` 建 `images` 文件夹。
2. 把 `C:\Users\zy147\AppData\Local\Temp\` 下那些 `1784XXXXXXXXX.png` 全部复制进 `D:\Newcode\images\`（文件名不变）。
3. Typora 打开 md → 确认图片和流程图都显示 → 导出 PDF。

## Must-NOT-Have
- 不改任何正文文字、代码、表格内容。
- 不改图片的数字文件名（保留，方便用户直接复制）。
- 不补回之前丢失的章节内容（用户已明确不补）。
- 不改 mermaid 流程图（已修好）。
- 不动其他文件。
