# RM 战队 STM32 开发环境圣经｜脱离 Keil，拥抱现代工具链

### CMake + Ninja + GCC 极速编译 · clangd 智能提示 · DAP/J-Link 双烧录器自动切换 · Ozone 高速调试 · 多工程一键流

从一台空白 Windows 到"多工程一键编译烧录调试 + 智能补全"的完整环境，全流程手把手。速查风格：做什么 + 命令 + 一句话结果。单根 / 多根工作区通吃，一套快捷键统治所有工程。

---

## 第0章 适用范围

- 芯片：STM32F405RGT6（Cortex-M4，单精度 FPU）
- 系统：Windows
- 烧录器：CMSIS-DAP
- 烧录 / 调试：OpenOCD
- 构建：CMake + Ninja
- 编译器：arm-none-eabi-gcc

从 Keil 迁移过来的要点：Keil 把 `.c`/`.cpp` 都当 C++ 编，GCC 严格按扩展名判断（`.c` 当 C，`.cpp` 当 C++）。这个差别的坑在第3、9、10章处理。

> 本文贴的工程配置都是逐字从真实文件抄的，可直接对照核对。

---

## 第1章 下载清单（四件套）

装这四样，各自下载地址如下：

- **arm-none-eabi-gcc**：把 C/C++ 编成 STM32 机器码。<https://developer.arm.com/downloads/-/arm-gnu-toolchain-downloads>（选 Windows 的 `arm-none-eabi` 包，推荐 `.exe`）
- **CMake**：读工程配置生成构建脚本。<https://cmake.org/download/>（选 `Windows x64 Installer`，装时勾 Add to PATH）
- **Ninja**：真正干活的构建执行器。<https://github.com/ninja-build/ninja/releases>（或装 STM32CubeCLT 自带）
- **OpenOCD**：烧录 / 调试。<https://github.com/xpack-dev-tools/openocd-xpack/releases>（下 Windows x64 压缩包解压）

> 避坑必需：装过 STM32CubeCLT 的话，ninja 可能已在 bundle 目录 `C:\Users\<用户名>\AppData\Local\stm32cube\bundles\ninja\1.13.1+st.1\bin`，把它加进 PATH 即可，不用再单独下（版本号可能不同，进目录看一眼）。

---

## 第2章 配置 PATH 环境变量

把四个工具的 `bin` 目录加进用户级 PATH。

> ⚠ 唯一必须记住的坑：**别用 `setx`**。它会把整条 PATH 截断到 1024 字符，超出部分被砍掉，很多软件会突然失效且难查。

安全写法（PowerShell 普通权限，逐个加，只改第一行路径）：

```powershell
# Ninja（CubeCLT 目录，按实际改）
$new = "C:\Users\<用户名>\AppData\Local\stm32cube\bundles\ninja\1.13.1+st.1\bin"
$old = [Environment]::GetEnvironmentVariable('Path', 'User')
if ($old -notlike "*$new*") { [Environment]::SetEnvironmentVariable('Path', $old + ';' + $new, 'User') }
```

```powershell
# arm-none-eabi-gcc（按实际安装路径改）
$new = "C:\Program Files (x86)\Arm GNU Toolchain arm-none-eabi\15.2 rel1\bin"
$old = [Environment]::GetEnvironmentVariable('Path', 'User')
if ($old -notlike "*$new*") { [Environment]::SetEnvironmentVariable('Path', $old + ';' + $new, 'User') }
```

```powershell
# CMake（安装时勾了加 PATH 就可省）
$new = "C:\Program Files\CMake\bin"
$old = [Environment]::GetEnvironmentVariable('Path', 'User')
if ($old -notlike "*$new*") { [Environment]::SetEnvironmentVariable('Path', $old + ';' + $new, 'User') }
```

```powershell
# OpenOCD（按解压路径改）
$new = "C:\tools\xpack-openocd-0.12.0\bin"
$old = [Environment]::GetEnvironmentVariable('Path', 'User')
if ($old -notlike "*$new*") { [Environment]::SetEnvironmentVariable('Path', $old + ';' + $new, 'User') }
```

改完 **关掉重开所有终端 / VSCode**（环境变量只对新进程生效）。然后验证，四条都打出版本号就 OK：

```powershell
arm-none-eabi-gcc --version
cmake --version
ninja --version
openocd --version
```

哪条报“无法识别”，就是那个工具没进 PATH，回头检查路径是否写到 `bin` 那一层。

---

## 第3章 CMakeLists.txt（主构建脚本）

本工程 `CMakeLists.txt` 真实内容（逐字照抄）：

```cmake
cmake_minimum_required(VERSION 3.22)

#
# This file is generated only once,
# and is not re-generated if converter is called multiple times.
#
# User is free to modify the file as much as necessary
#

# Setup compiler settings
set(CMAKE_C_STANDARD 11)
set(CMAKE_C_STANDARD_REQUIRED ON)
set(CMAKE_C_EXTENSIONS ON)
set(CMAKE_CXX_STANDARD 17)
set(CMAKE_CXX_STANDARD_REQUIRED ON)
set(CMAKE_CXX_EXTENSIONS ON)


# Define the build type
if(NOT CMAKE_BUILD_TYPE)
    set(CMAKE_BUILD_TYPE "Debug")
endif()

# Set the project name
set(CMAKE_PROJECT_NAME Gimbal_Demo)

# Include toolchain file
include("cmake/gcc-arm-none-eabi.cmake")

# Enable compile command to ease indexing with e.g. clangd
set(CMAKE_EXPORT_COMPILE_COMMANDS TRUE)

# Enable CMake support for ASM, C and C++ languages
enable_language(C CXX ASM)

# Core project settings
project(${CMAKE_PROJECT_NAME})
message("Build type: " ${CMAKE_BUILD_TYPE})

# Create an executable object type
add_executable(${CMAKE_PROJECT_NAME})

# Add STM32CubeMX generated sources
add_subdirectory(cmake/stm32cubemx)

# Force C files containing C++ code to compile as C++ (do not rename these files)
set_source_files_properties(${CMAKE_SOURCE_DIR}/Core/Src/main.c ${CMAKE_SOURCE_DIR}/Core/Src/stm32f4xx_it.c ${CMAKE_SOURCE_DIR}/RM2023_Lib_V1.2/communication.c ${CMAKE_SOURCE_DIR}/RM2023_Lib_V1.2/my_math.c PROPERTIES LANGUAGE CXX)

# Relax C++ strictness ONLY for the C-code-compiled-as-C++ files (preserves Keil-lenient behavior; no codegen change)
set_source_files_properties(
    ${CMAKE_SOURCE_DIR}/Core/Src/main.c
    ${CMAKE_SOURCE_DIR}/Core/Src/stm32f4xx_it.c
    ${CMAKE_SOURCE_DIR}/RM2023_Lib_V1.2/communication.c
    ${CMAKE_SOURCE_DIR}/RM2023_Lib_V1.2/my_math.c
    PROPERTIES COMPILE_OPTIONS "-fpermissive"
)

# Link directories setup
target_link_directories(${CMAKE_PROJECT_NAME} PRIVATE
    # Add user defined library search paths
)

# Add sources to executable
target_sources(${CMAKE_PROJECT_NAME} PRIVATE
    # Add user sources here
    RM2023_Lib_V1.2/RM_Lib.cpp
    RM2023_Lib_V1.2/rgb_debug.cpp
    RM2023_Lib_V1.2/my_math.c
    RM2023_Lib_V1.2/CP_System.c
    RM2023_Lib_V1.2/communication.c
    Core/Src/example_data.c
    Core/Src/nmea_dec.c
    Core/Src/hipnuc_dec.c
)

# Add include paths
target_include_directories(${CMAKE_PROJECT_NAME} PRIVATE
    # Add user defined include paths
    RM2023_Lib_V1.2
)

# Add project symbols (macros)
target_compile_definitions(${CMAKE_PROJECT_NAME} PRIVATE
    # Add user defined symbols
)

# Add linked libraries
target_link_libraries(${CMAKE_PROJECT_NAME}
    stm32cubemx

    # Add user defined libraries
)

# Generate .hex and .bin, and print size after build
add_custom_command(TARGET ${CMAKE_PROJECT_NAME} POST_BUILD
    COMMAND ${CMAKE_OBJCOPY} -O ihex $<TARGET_FILE:${CMAKE_PROJECT_NAME}> ${CMAKE_PROJECT_NAME}.hex
    COMMAND ${CMAKE_OBJCOPY} -O binary -S $<TARGET_FILE:${CMAKE_PROJECT_NAME}> ${CMAKE_PROJECT_NAME}.bin
    COMMAND ${CMAKE_SIZE} $<TARGET_FILE:${CMAKE_PROJECT_NAME}>
)
```

改这里干什么：

- **加源文件**：自己写的 `.cpp`/`.c` 往 `target_sources` 里加（CubeMX 生成的不在这加，见第9章）。
- **加 include 路径**：往 `target_include_directories` 里加，本工程加了 `RM2023_Lib_V1.2`。
- **强制 `.c` 按 C++ 编（关键坑）**：`main.c` / `stm32f4xx_it.c` / `communication.c` / `my_math.c` 名字是 `.c` 但里面有 `class` 等 C++ 语法。用 `set_source_files_properties(... PROPERTIES LANGUAGE CXX)` 强制它们用 g++ 编，恢复 Keil 行为，且不用改文件名。
- **`-fpermissive`**：只给上面四个文件加，把“C 合法、C++ 报错”（如指针隐式转换）降级成警告。正常 `.cpp` 不受影响。
- **POST_BUILD 生成 hex/bin**：顺便产物，本工程烧 `.elf` 不需要它们，备用而已。

---

## 第4章 工具链文件 gcc-arm-none-eabi.cmake

指定编译器和 STM32F405 的芯片参数。本工程 `cmake/gcc-arm-none-eabi.cmake` 真实内容（逐字照抄）：

```cmake
set(CMAKE_SYSTEM_NAME               Generic)
set(CMAKE_SYSTEM_PROCESSOR          arm)

set(CMAKE_C_COMPILER_FORCED TRUE)
set(CMAKE_CXX_COMPILER_FORCED TRUE)
set(CMAKE_C_COMPILER_ID GNU)
set(CMAKE_CXX_COMPILER_ID GNU)

# Some default GCC settings
# arm-none-eabi- must be part of path environment
set(TOOLCHAIN_PREFIX                arm-none-eabi-)

set(CMAKE_C_COMPILER                ${TOOLCHAIN_PREFIX}gcc)
set(CMAKE_ASM_COMPILER              ${CMAKE_C_COMPILER})
set(CMAKE_CXX_COMPILER              ${TOOLCHAIN_PREFIX}g++)
set(CMAKE_LINKER                    ${TOOLCHAIN_PREFIX}g++)
set(CMAKE_OBJCOPY                   ${TOOLCHAIN_PREFIX}objcopy)
set(CMAKE_SIZE                      ${TOOLCHAIN_PREFIX}size)

set(CMAKE_EXECUTABLE_SUFFIX_ASM     ".elf")
set(CMAKE_EXECUTABLE_SUFFIX_C       ".elf")
set(CMAKE_EXECUTABLE_SUFFIX_CXX     ".elf")

set(CMAKE_TRY_COMPILE_TARGET_TYPE STATIC_LIBRARY)

# MCU specific flags
set(TARGET_FLAGS "-mcpu=cortex-m4 -mfpu=fpv4-sp-d16 -mfloat-abi=hard ")

set(CMAKE_C_FLAGS "${CMAKE_C_FLAGS} ${TARGET_FLAGS}")
set(CMAKE_C_FLAGS "${CMAKE_C_FLAGS} -Wall -Wextra -Wpedantic -fdata-sections -ffunction-sections")

# C++ inherits the common C flags (TARGET_FLAGS + warnings/sections) and adds C++-only flags
set(CMAKE_CXX_FLAGS "${CMAKE_C_FLAGS} -fno-rtti -fno-exceptions -fno-threadsafe-statics")

# Per-build-type optimization/debug flags applied to BOTH C and C++ so C++ files get them too
if(CMAKE_BUILD_TYPE MATCHES Debug)
    set(CMAKE_C_FLAGS "${CMAKE_C_FLAGS} -O0 -g3")
    set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -O0 -g3")
endif()
if(CMAKE_BUILD_TYPE MATCHES Release)
    set(CMAKE_C_FLAGS "${CMAKE_C_FLAGS} -Os -g0")
    set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -Os -g0")
endif()

set(CMAKE_ASM_FLAGS "${CMAKE_C_FLAGS} -x assembler-with-cpp -MMD -MP")

set(CMAKE_C_LINK_FLAGS "${TARGET_FLAGS}")
set(CMAKE_C_LINK_FLAGS "${CMAKE_C_LINK_FLAGS} -T \"${CMAKE_SOURCE_DIR}/STM32F405RGTx_FLASH.ld\"")
set(CMAKE_C_LINK_FLAGS "${CMAKE_C_LINK_FLAGS} --specs=nano.specs")
# newlib-nano strips float support from printf/scanf by default; force it back in
set(CMAKE_C_LINK_FLAGS "${CMAKE_C_LINK_FLAGS} -u _printf_float")
set(CMAKE_C_LINK_FLAGS "${CMAKE_C_LINK_FLAGS} -Wl,-Map=${CMAKE_PROJECT_NAME}.map -Wl,--gc-sections")
set(CMAKE_C_LINK_FLAGS "${CMAKE_C_LINK_FLAGS} -Wl,--start-group -lc -lm -Wl,--end-group")
set(CMAKE_C_LINK_FLAGS "${CMAKE_C_LINK_FLAGS} -Wl,--print-memory-usage")

set(CMAKE_CXX_LINK_FLAGS "${CMAKE_C_LINK_FLAGS} -Wl,--start-group -lstdc++ -lsupc++ -Wl,--end-group")
```

要点：

- `TARGET_FLAGS` 那三个（`-mcpu=cortex-m4 -mfpu=fpv4-sp-d16 -mfloat-abi=hard`）必须和芯片对上，编译和链接前后一致，否则报浮点 ABI 不匹配。
- **优化 / 调试 flag 必须同时加到 C 和 C++（易踩的坑）**：每个 `if` 里都写了两行，`-O0 -g3` 同时进 `CMAKE_C_FLAGS` 和 `CMAKE_CXX_FLAGS`。因为 `CMAKE_CXX_FLAGS` 早在上面就基于当时的 `CMAKE_C_FLAGS` 定型了；只加 C 那边，`.cpp` 就没优化 / 没调试信息（能编能烧，但 C++ 打不了断点或体积异常）。
- **`-u _printf_float`（`printf`/`sprintf` 打浮点必需）**：`--specs=nano.specs` 用的是精简版 newlib，`sprintf` 默认**不含浮点格式化代码**（为省 Flash）。不加这行，`sprintf(buf, "%.2f", x)` 里的 `%f` 会输出空——INFO 打 `"%f,%f,%f\n"` 串口就只收到 `,,`。加上 `-u _printf_float` 强制把浮点代码链进来即可，代价是 Flash 多占几 KB（F405 有 1MB，可忽略）。详见第10章第 11 条。

---

## 第5章 openocd.cfg

本工程 `openocd.cfg` 真实内容（就 4 行）：

```
source [find interface/cmsis-dap.cfg]
transport select swd
source [find target/stm32f4x.cfg]
adapter speed 4000
```

- 第1行：加载 CMSIS-DAP 烧录器配置。
- 第2行：通信协议选 SWD。
- 第3行：加载 STM32F4 目标芯片配置（F405 属于 F4）。
- 第4行：烧录时钟 4 MHz（稳妥值）。

烧 `.elf` 不用先生成 hex/bin：elf 自带每段的加载地址，OpenOCD `program` 直接按地址写。

### 5.1 用 J-Link 而非 CMSIS-DAP

最省事、也是本工程实际采用的方案是**同一个快捷键自动识别烧录器**：插 DAP 就用 DAP 烧、插 J-Link 就用 J-Link 烧，什么都不用切。做法见 **第11章**（强烈推荐直接看那章）。

如果你只想临时用 J-Link、不搞自动切换，也可以把 `openocd.cfg` 第一行 `source [find interface/cmsis-dap.cfg]` 换成 `source [find interface/jlink.cfg]`，其余三行不动。**但注意**：这需要把 J-Link 的 USB 驱动切成 WinUSB（否则 OpenOCD 报 `LIBUSB_ERROR_NOT_FOUND`），会和 SEGGER 官方工具冲突——具体原因和更好的解法都在第11章讲清楚了。

---

## 第6章 一键任务 tasks.json

本工程 `.vscode/tasks.json` 真实内容（逐字照抄）：

```json
{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "cmake --build --preset Debug",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": ["$gcc"]
        },
        {
            "label": "flash",
            "type": "shell",
            "command": "openocd -f openocd.cfg -c \"program build/Debug/Gimbal_Demo.elf verify reset exit\"",
            "problemMatcher": []
        },
        {
            "label": "build and flash",
            "dependsOrder": "sequence",
            "dependsOn": [
                "build",
                "flash"
            ],
            "problemMatcher": []
        }
    ]
}
```

三个任务：

- **build**：`cmake --build --preset Debug` 编译，是默认构建任务（可被 `Ctrl+Shift+B` 触发）。首次会先配置生成 Ninja 脚本。
- **flash**：OpenOCD 把 `build/Debug/Gimbal_Demo.elf` 写进 Flash，`verify` 校验、`reset` 复位、`exit` 退出。路径的 `Debug` 和 build 预设一致，`Gimbal_Demo` 就是 `CMAKE_PROJECT_NAME`。
- **build and flash**：`dependsOn` 按顺序先 build 再 flash，一键编译加烧录。

> 编译报错会进 VSCode “问题”面板（`Ctrl+Shift+M`），终端里的 `文件:行:列` 可 `Ctrl+点击` 直接跳到出错行，且红 / 黄颜色区分错误和警告。（这是因为 build 任务配了 `$gcc` 的 problemMatcher。）

---

## 第7章 快捷键 Alt+, / Alt+N / Alt+M

- `Alt+,` → `build`
- `Alt+N` → `flash`
- `Alt+M` → `build and flash`

打开方式：`Ctrl+Shift+P` → `Preferences: Open Keyboard Shortcuts (JSON)`（打开的是当前激活 profile 的 keybindings.json）。

> 坑：VSCode 每个 profile 有独立 keybindings.json，写错 profile 就没反应。别选带 `Default` 的那个（内置只读表，改了存不下）。

三条 keybinding（本工程真实配置，逐字照抄）：

```json
{
    "key": "alt+oem_comma",
    "command": "workbench.action.tasks.runTask",
    "args": "build"
},
{
    "key": "alt+n",
    "command": "workbench.action.tasks.runTask",
    "args": "flash"
},
{
    "key": "alt+m",
    "command": "workbench.action.tasks.runTask",
    "args": "build and flash"
}
```

`alt+oem_comma` 就是 `Alt+,`。`args` 要和第6章任务的 `label` 一字不差。

若 `Alt+,` 被 GitLens 抢了，用 `-命令名`（命令前加减号）解绑：

```json
{
    "key": "alt+oem_comma",
    "command": "-gitlens.diffWithPrevious:key",
    "when": "editorTextFocus && config.gitlens.keymap == 'alternate' && resource in 'gitlens:tabs:tracked'"
},
{
    "key": "alt+oem_comma",
    "command": "-gitlens.key.alt+,",
    "when": "gitlens:key:,"
}
```

---

## 第8章 编译 / 烧录操作

- **编译**：`Ctrl+Shift+B` 或 `Alt+,`。首次稍慢（要先配置），之后只重编改动的。编完终端打印固件大小。
- **烧录**：先插好 CMSIS-DAP 接板子。单独烧按 `Alt+N`；改完一步到位按 `Alt+M`（先编再烧）。

### 8.1 怎么看编译错误

- 只看带 `error:` 的行，忽略 `warning:`（本工程开了 `-Wall -Wextra -Wpedantic`，警告多很正常）。
- 报错格式 `文件:行:列: error: 信息`，例：

  ```
  Core/Src/main.c:123:5: error: expected ';' before '}' token
  ```

  即 `main.c` 第 123 行第 5 列缺分号。
- 从下往上找**第一个** `error:`，通常是根源，修好再重编能连带消掉一串。
- 报错会进 VSCode “问题”面板（`Ctrl+Shift+M`），终端里的 `文件:行:列` 可 `Ctrl+点击` 直接跳到出错行，红 / 黄颜色区分错误和警告（build 任务配了 `$gcc` 的 problemMatcher）。

---

## 第9章 与 STM32CubeMX 共存

CubeMX 重新生成代码时会覆盖它管辖的文件，所以改动放对地方。

### 9.1 用户改动放哪

- 放：主 **`CMakeLists.txt`**（第3章）和工具链文件 **`cmake/gcc-arm-none-eabi.cmake`**（第4章）。
- **别改** `cmake/stm32cubemx/CMakeLists.txt`（CubeMX 生成并每次覆盖，加的东西会丢）。

### 9.2 生成的文件不改名、不改内容

- `main.c` / `stm32f4xx_it.c` 等**不要改名**（别改成 `.cpp`，会跟 CubeMX 打架）。
- 写业务逻辑用 `USER CODE BEGIN / END` 区块（重新生成时保留）。
- `.c` 里的 C++ 代码要按 C++ 编，只在主 CMakeLists.txt 用 `LANGUAGE CXX`（第3章），文件一个字不动。

---

## 第10章 常见问题 / 避坑合集

**1. `.c` 里 C++ 代码编不过**：报 `unknown type name 'class'`。GCC 按扩展名把 `.c` 当 C 编。→ 用 `set_source_files_properties(文件 PROPERTIES LANGUAGE CXX)`（第3章）。

**2. 指针隐式转换报错**：`invalid conversion from 'int32_t*' to 'int*'`。C++ 检查更严。→ 给这些文件加 `-fpermissive`（第3章）。

**3. 找不到 Ninja**：`无法将"ninja"识别为...` 或找不到 Ninja generator。Ninja 没进 PATH。→ 加进 PATH（CubeCLT 的在 `...\stm32cube\bundles\ninja\1.13.1+st.1\bin`），重开终端 / VSCode（第2章）。

**4. 快捷键按了没反应**：keybinding 写进了没激活的 profile。→ 用 `Open Keyboard Shortcuts (JSON)` 打开当前 profile 的文件再写（第7章）。

**5. 键位表只读改不了**：打开成了带 `Default` 的内置表。→ 改用不带 `Default` 的命令（第7章）。

**6. 只有 Alt+, 不灵**：被 GitLens 抢了。→ 用 `-命令名` 解绑（第7章）。

**7. “打开文件夹” vs “工作区” 行为不一样**：两种方式读的 settings 不同，工作区可能切到别的 profile 让快捷键“消失”。（历史上用过 EIDE 的话，`.vscode` 里可能残留 `configurationProvider` 指向 `cl.eide` 的旧设置影响 IntelliSense，一起清掉。）→ 统一用“打开文件夹”，配置放 `.vscode/`。

**8. 类型处一堆红波浪线（能编过但报红）**：IntelliSense 默认把 `.c` 当纯 C 解析。→ 在 `.vscode/settings.json`：

```json
"files.associations": {
    "*.c": "cpp",
    "*.h": "cpp"
},
"C_Cpp.default.compileCommands": "${workspaceFolder}/build/Debug/compile_commands.json"
```

改完运行 `C/C++: Reset IntelliSense Database`（前提是编译过一次，`compile_commands.json` 才存在）。

**9. “没有找到要运行的任务”**：打开的目录层级不对，读不到 `.vscode/tasks.json`。→ 精确“打开文件夹”到含 tasks.json 那层：

```
D:\RM_RMUC26_Guosai\Gimbal_24hero_CH010_20260120\Gimbal_24hero_CH010_20260111
```

**10. 报错太多定位难**：只看 `error:` 忽略 `warning:`，从下往上找第一个 `error:` 按 `文件:行:列` 跳过去（第8章）。

**11. 串口打浮点数打出来是空 / 只剩逗号（GCC 不能打印浮点数）**：`INFO("%f,%f,%f\n", a, b, c)` 串口收到的是 `,,`——三个 `%f` 全空，只剩两个分隔逗号；换 `"x=%.2f"` 就只剩 `x=`。**能编能烧、不报任何错**，纯运行期现象，极具迷惑性。

- **根因**：本工程 `INFO` 宏底层是 `sprintf`（见 `RM2023_Lib_V1.2/RM_Lib.h`：`sprintf((char*)info_ubuf, __VA_ARGS__)`，云台走 huart1、底盘走 huart2）。而链接用了 `--specs=nano.specs`（精简版 newlib），它的 `sprintf` **默认不带浮点格式化代码**（省 Flash），于是所有 `%f`/`%e`/`%g` 都被忽略、输出空。整数 `%d`、字符串 `%s` 不受影响，所以很容易误判成"数据本身是 0"或"串口坏了"。
- **怎么确认是它**：把 `%f` 临时换成 `%d`（如 `INFO("%d\n", (int)(x*100))`），若整数能正常打出，就是浮点支持没链进来，不是数据问题、不是波特率问题（波特率错会满屏乱码，不是整齐的 `,,`）。
- **解法**：给链接器加 `-u _printf_float`，强制把浮点代码链进来。在 `cmake/gcc-arm-none-eabi.cmake` 的 `--specs=nano.specs` 那行后面加一行（第4章已包含）：

  ```cmake
  set(CMAKE_C_LINK_FLAGS "${CMAKE_C_LINK_FLAGS} -u _printf_float")
  ```

  **改的是链接标志，必须重新配置 CMake**（光重编不重跑 configure 不生效）：删 `build/` 重来一次最稳，`Remove-Item -LiteralPath build -Recurse -Force` 后再 `Alt+,`（原理同第14.2章）。重烧后串口就能看到 `-0.31,-0.48,-1.21` 这样的实际数值。代价是 Flash 多占几 KB，对 F405（1MB）可忽略。
- **顺带**：`scanf` 读浮点同理要 `-u _scanf_float`，但嵌入式几乎不用 `scanf`，本工程没加。

---

至此从空白电脑到一键编译 / 烧录就配完了。

---

## 第11章 双烧录器自动切换（DAP + J-Link，同一个快捷键，插谁烧谁）

这一章是本工程的一个亮点：**`Alt+N`（flash）和 `Alt+M`（build and flash）不用动，插上哪个烧录器就自动用哪个**。插正点原子 DAP 就走 OpenOCD，插 J-Link 就走 SEGGER 原生工具，全程你不用改任何配置、不用记"现在插的是哪个"。

### 11.1 为什么不是"换一行 openocd.cfg"那么简单

第5.1章那种"把 `cmsis-dap.cfg` 换成 `jlink.cfg`"的办法，理论上可行，但实际会踩一个硬坑：

- **驱动打架**。你装 SEGGER 官方 J-Link 软件包后，J-Link 用的是 SEGGER 私有内核驱动（设备管理器里驱动服务名是 `jlink`）。而 **OpenOCD 走的是 libusb**，它要求设备绑定到 **WinUSB** 才能打开。两者要的驱动不是一个，于是 OpenOCD 直接报：

  ```
  Warn : Failed to open device: LIBUSB_ERROR_NOT_FOUND
  Error: No J-Link device found
  ```

- 要让 OpenOCD 用 J-Link，就得用 Zadig 工具把 J-Link 的接口驱动从 `jlink` 换成 `WinUSB`——但换完，SEGGER 自家的 JLink.exe / J-Flash 反而用不了了，来回切很烦。

**结论**：DAP 天生免驱（CMSIS-DAP 走 HID），OpenOCD 用它没问题；但 J-Link 硬要塞进 OpenOCD 得折腾驱动。所以本工程的方案是——**各用各最顺的工具，用一个脚本在幕后自动分流**：

| 插的烧录器 | 幕后用什么烧 | 为什么 |
| --- | --- | --- |
| CMSIS-DAP（正点原子） | OpenOCD | 免驱，原流程一字不改 |
| J-Link V9 | SEGGER 原生 `JLink.exe` | 直接用已装的 SEGGER 驱动，不用碰 WinUSB，且比 OpenOCD 烧得快 |

**牛逼与众不同之处**：对你来说永远只有一个快捷键、一个操作；工具的差异、驱动的坑，全被那个检测脚本吃掉了。既保留了 DAP 的零改动，又拿到了 J-Link 的原生速度，还不牺牲 SEGGER 官方工具的可用性。

### 11.2 前置：装 J-Link 驱动（关键，容易装错）

去 <https://www.segger.com/downloads/jlink/> 下 **"J-Link Software and Documentation pack for Windows"**（不是 Ozone，Ozone 只是调试 GUI，不含 USB 驱动）。装完插上 J-Link，设备管理器里能看到一个驱动服务名为 `jlink`、状态正常的设备就 OK。

> ⚠ 便宜的兼容 / OB 版 V9 常见坑：装最新版可能被判"非正品固件"而罢工；弹"update/replace firmware"提示时选**否/取消**。稳妥做法是按商家指定的版本装。

验证 J-Link 能连芯片（接好 SWD 线后）——用 SEGGER 自带的 JLink.exe 跑一次，看到 `Found Cortex-M4` 就说明硬件通了：

```powershell
# 把路径换成你实际的 JLink.exe 位置
& "D:\SOFTWARE\JLink_V960\JLink.exe" -CommanderScript <脚本> -NoGui 1
```

> 若报 `Failed to initialize DAP` / `Cannot connect to target`：不是驱动问题，是 **SWD 线没接好**（SWCLK / SWDIO / GND 三根要接牢，参照 DAP 怎么接的）。另外**别把 DAP 和 J-Link 同时插在板子 SWD 上**，两个烧录器抢总线会互相干扰，只留一个。

### 11.3 检测脚本 scripts/flash.ps1（逐字照抄）

核心就是这个脚本：扫 USB 找 SEGGER 的厂商号 `VID_1366`，找到就用 JLink.exe 烧 `.hex`，否则回退 OpenOCD 烧 `.elf`。

```powershell
<#
.SYNOPSIS
    Auto-detect flasher (J-Link or CMSIS-DAP) and flash firmware.

.DESCRIPTION
    Same single shortcut, "plug in and flash". Scans USB for a Segger J-Link
    (VID 0x1366). If present, flashes with Segger's own JLink.exe (native,
    fast, uses the installed Segger driver). Otherwise falls back to OpenOCD
    with the CMSIS-DAP config. The DAP path is unchanged from before.

.PARAMETER Elf
    Path to the .elf (OpenOCD/DAP path). Default build/Debug/Gimbal_Demo.elf
.PARAMETER Hex
    Path to the .hex (JLink.exe path). Default build/Debug/Gimbal_Demo.hex
#>
param(
    [string]$Elf = "build/Debug/Gimbal_Demo.elf",
    [string]$Hex = "build/Debug/Gimbal_Demo.hex"
)

$ErrorActionPreference = "Stop"

# Project root = parent of this script's directory (scripts/ -> root)
$projectRoot = Split-Path -Parent $PSScriptRoot
Set-Location -LiteralPath $projectRoot

# --- Detect J-Link (VID_1366) --------------------------------------------
# 用 -PresentOnly 只认"当前物理插着"的 J-Link；不加会把已拔掉的 J-Link 幽灵设备也算上。
# 不按 Status 过滤：J-Link 的 composite 子节点 Status 会随枚举时机在 OK/Unknown 间抖动。
# 脚本务必保持纯 ASCII（PS 5.1 按系统 ANSI/GBK 读，UTF-8 中文注释会崩，详见 11.7）。
$jlink = Get-PnpDevice -PresentOnly -ErrorAction SilentlyContinue |
    Where-Object { $_.InstanceId -match "VID_1366" } |
    Select-Object -First 1

if ($jlink) {
    # ---- J-Link path: use Segger's native JLink.exe -----------------------
    Write-Output "[flash] Detected J-Link (VID_1366), using Segger JLink.exe"

    $jlinkExe = (Get-Command JLink.exe -ErrorAction SilentlyContinue).Source
    if (-not $jlinkExe) {
        $fallback = "D:\SOFTWARE\JLink_V960\JLink.exe"
        if (Test-Path -LiteralPath $fallback) { $jlinkExe = $fallback }
        else { Write-Error "[flash] JLink.exe not found in PATH or $fallback"; exit 1 }
    }
    if (-not (Test-Path -LiteralPath $Hex)) {
        Write-Error "[flash] HEX not found: $Hex (build first?)"; exit 1
    }

    # Build a JLink CommanderScript: connect, program+verify, reset+run, exit
    $hexAbs = (Resolve-Path -LiteralPath $Hex).Path
    $script = @"
si SWD
speed 8000
device STM32F405RG
connect
loadfile "$hexAbs"
r
g
exit
"@
    $tmp = Join-Path $env:TEMP "flash_jlink_$PID.jlink"
    $script | Out-File -FilePath $tmp -Encoding ascii
    try {
        & $jlinkExe -CommanderScript $tmp -ExitOnError 1 -NoGui 1
        $code = $LASTEXITCODE
    } finally {
        Remove-Item $tmp -ErrorAction SilentlyContinue
    }
    exit $code
}

# ---- DAP path: OpenOCD (unchanged behavior) ------------------------------
Write-Output "[flash] No J-Link found, using CMSIS-DAP + OpenOCD"

$openocd = (Get-Command openocd -ErrorAction SilentlyContinue).Source
if (-not $openocd) {
    $fallback = "C:\Tools\xpack-openocd-0.12.0-7-win32-x64\xpack-openocd-0.12.0-7\bin\openocd.exe"
    if (Test-Path -LiteralPath $fallback) { $openocd = $fallback }
    else { Write-Error "[flash] openocd not found in PATH or $fallback"; exit 1 }
}
if (-not (Test-Path -LiteralPath $Elf)) {
    Write-Error "[flash] ELF not found: $Elf (build first?)"; exit 1
}

Write-Output "[flash] Config: openocd.cfg | ELF: $Elf"
& $openocd -f "openocd.cfg" -c "program `"$Elf`" verify reset exit"
exit $LASTEXITCODE
```

脚本逐段在干什么：

- **定位工程根**：脚本在 `scripts/` 下，取上一级当工程根并 `Set-Location`，这样不管从哪触发，相对路径 `build/Debug/...`、`openocd.cfg` 都对得上。
- **检测 J-Link**：`Get-PnpDevice` 扫 USB，匹配 SEGGER 厂商号 `VID_1366` 且状态 `OK` 的设备。这一步只有几十毫秒，感知不到。
- **J-Link 分支**：找到 J-Link → 找 `JLink.exe`（先查 PATH，找不到回退到硬编码安装路径）→ 生成一个临时 `.jlink` 命令脚本（选 SWD、8MHz、指定芯片、连接、烧 hex、复位、运行、退出）→ 调 `JLink.exe -CommanderScript` 执行 → 用完删临时文件 → 透传退出码。烧 `.hex` 是因为它自带地址，JLink.exe 烧最省事。
- **DAP 分支（回退）**：没找到 J-Link → 完全走你原来的 OpenOCD 命令烧 `.elf`，行为一字不变。
- **两处 `$fallback` 路径**：`JLink.exe` 默认回退 `D:\SOFTWARE\JLink_V960\JLink.exe`，`openocd` 默认回退到解压目录。**如果你的安装路径不同，改这两行即可**（或把它们加进 PATH，脚本会优先用 PATH 里的）。

### 11.4 tasks.json 里 flash 任务改成调脚本（逐字照抄）

把第6章 `flash` 任务的 command 从写死的 openocd 命令，改成调用脚本：

```json
{
    "label": "flash",
    "type": "shell",
    "command": "powershell -ExecutionPolicy Bypass -File \"${workspaceFolder}\\scripts\\flash.ps1\"",
    "problemMatcher": []
}
```

`build` 和 `build and flash` 两个任务**完全不用动**——`build and flash` 本来就是 `dependsOn: [build, flash]`，flash 改了它自动继承，`Alt+M` 依旧一键编译加烧录。

> ⚠ 必踩的坑：`${workspaceFolder}` 里含反斜杠，**路径一定要加引号**。不加引号会被 shell 把反斜杠吃掉，报：
>
> ```
> -File 形式参数的实际参数"D:NewcodeGimbal_..."不存在
> ```
>
> （`D:\Newcode\...` 变成了 `D:Newcode...`）。所以上面用了 `\"${workspaceFolder}\\scripts\\flash.ps1\"`——外层引号保住完整路径，`\\` 是 JSON 里的反斜杠转义。

### 11.5 用法和验证

- 用法：跟以前一模一样。插好烧录器（**只插一个**），`Alt+N` 单独烧、`Alt+M` 编译加烧。终端第一行会打印它选了哪条路：
  - `[flash] Detected J-Link (VID_1366), using Segger JLink.exe`
  - `[flash] No J-Link found, using CMSIS-DAP + OpenOCD`
- 实测 J-Link 烧录输出（成功长这样）：

  ```
  Found Cortex-M4 r0p1
  Downloading file [...Gimbal_Demo.hex]...
  Programming flash [100%] Done.
  Verifying flash   [100%] Done.
  O.K.
  J-Link: Flash download: Total: 1.001s ... Program & Verify speed: 145 KB/s
  ```

- 速度：J-Link 走 JLink.exe 是 SEGGER 原生算法，比走 OpenOCD 快；脚本检测那几十毫秒可忽略。一来一回，用 J-Link 反而更快，绝不会更慢。

### 11.6 本章新增 / 改动的文件小结

- **新增** `scripts/flash.ps1`：自动检测 + 分流的核心脚本。
- **改** `.vscode/tasks.json`：`flash` 任务 command 指向脚本（见 11.4）；`build` / `build and flash` 不变。
- **未动** `openocd.cfg`、`launch.json`、固件代码：DAP 烧录、cortex-debug 调试全保持原样。
- 说明：调试（`launch.json` 的 cortex-debug）仍固定走 DAP，本方案只做**烧录**的自动切换；想让调试也自动切换要另配，暂未做。

### 11.7 双烧录器排障合集（真实踩坑复盘，务必看）

这一节记录 flash.ps1 上线后真实踩到的一串坑，每一个都让"插上却烧不了"，且现象具有迷惑性。**结论先行：脚本靠"电脑上有没有 J-Link"分流，不靠"哪个接了板子"分流**，所以最稳的用法是——**电脑上一次只连一个烧录器**。

#### 坑1：脚本中文注释导致 PS 5.1 解析崩溃（最隐蔽）

- **现象**：插着 J-Link，脚本却打印 `[flash] No J-Link found, using CMSIS-DAP + OpenOCD`，然后 OpenOCD 报 `CMSIS-DAP command CMD_INFO failed` / `** OpenOCD init failed **`。手动跑 `Get-PnpDevice` 明明能查到 J-Link。
- **根因**：脚本存成 **UTF-8**，但 VSCode 任务用 **Windows PowerShell 5.1** 跑，5.1 默认按**系统 ANSI（简中机器是 GBK/936）**读取 `.ps1`。UTF-8 的中文字节被 GBK 曲解，某些字节序列里凑出一个**落单的双引号 `"`**，PowerShell 以为字符串没结束（`字符串缺少终止符`），从那行往下的代码（包括 `$jlink` 赋值）**全被吞掉** → `$jlink` 为空 → 走 DAP 分支。
- **迷惑点**：底盘工程能烧、云台不能——因为底盘那份脚本是纯英文，云台那份被加了中文注释。改 `-eq OK` 之类都没用，因为崩溃点在中文注释，不在检测条件。
- **定位命令**（模拟 5.1 的 GBK 读法，能否解析）：

  ```powershell
  powershell -Command "& { try { `$g=[Text.Encoding]::GetEncoding(936); `$t=`$g.GetString([IO.File]::ReadAllBytes('.\scripts\flash.ps1')); `$null=[ScriptBlock]::Create(`$t); 'PARSE OK' } catch { 'ERR: '+`$_.Exception.Message } }"
  ```

  输出 `PARSE OK` 才算安全；报 `字符串缺少终止符` 就是中了此坑。
- **解法（二选一，本工程用前者）**：
  1. **`.ps1` 保持纯 ASCII**——注释全用英文。检查有无非 ASCII 字节：

     ```powershell
     ($b=[IO.File]::ReadAllBytes('.\scripts\flash.ps1') | Where-Object { $_ -gt 127 }).Count   # 结果 0 才安全
     ```
  2. 或把 `.ps1` 存成 **带 BOM 的 UTF-8**（PS 5.1 见 BOM 会按 UTF-8 读），但团队协作里容易被编辑器改掉，不如纯 ASCII 稳。

#### 坑2：幽灵设备——拔了 J-Link 换 DAP，脚本还走 J-Link

- **现象**：拔掉 J-Link、插上 DAP，脚本仍打印 `Detected J-Link`，然后 JLink.exe 报 `Cannot connect to the probe/programmer`（探针根本不在了）。
- **根因**：`Get-PnpDevice` **不加 `-PresentOnly` 会连"曾经插过、现已拔掉"的幽灵设备一起列出**。拔掉的 J-Link 仍被算作"存在" → 脚本判定"有 J-Link" → 走错分支。
- **典型伴随**：`Get-PnpDevice | ? InstanceId -match VID_1366` 数出 2 个 J-Link，但你手上只有 1 个（1 真 1 幽灵）。
- **解法**：检测加 **`-PresentOnly`**，只认物理在线设备（11.3 脚本已改）。核对真实插着的：

  ```powershell
  Get-PnpDevice -PresentOnly | ? { $_.InstanceId -match "VID_1366|VID_0D28|VID_C251" } | ft Status,FriendlyName,InstanceId
  ```

#### 坑3：Status 抖动导致 `-eq "OK"` 间歇性漏检

- **现象**：同一个插着的 J-Link，脚本时而认得时而认不得。
- **根因**：J-Link 是 composite 设备，它的子节点（`MI_00`/`MI_02` 等）`Status` 会随枚举时机在 `OK` 和 `Unknown` 间跳。用 `-and $_.Status -eq "OK"` 过滤，在全为 `Unknown` 的瞬间就匹配为空 → 漏检 → 误走 DAP。
- **解法**：**不按 Status 过滤**，只要 `VID_1366` 存在（且 `-PresentOnly`）就判定 J-Link 在场（11.3 脚本已改）。

#### 坑4：两个烧录器都连着电脑，但只有一个接了板子（本质冲突）

- **现象**：DAP 接了板子、J-Link 只插了电脑没接板子；脚本检测到 J-Link 就走 J-Link → `Failed to initialize DAP` / `Could not connect to the target device`。
- **根因**：脚本只能判断"**电脑上有没有 J-Link**"，无法判断"**J-Link 有没有接板子**"。J-Link 的 USB 在线（`-PresentOnly` 也认它），于是抢走了本该给 DAP 的烧录。
- **解法（本工程采用）**：**电脑上一次只连一个烧录器**。烧云台就只连 J-Link，烧底盘就只连 DAP，另一个的 USB 从电脑上拔掉。想两个都常插、按工程固定烧录器，需要改脚本按 S/N 或固定分流，本工程暂不做。

#### 坑5：`Failed to initialize DAP`（连上探针但连不上芯片）

- **现象**：`Connecting to J-Link ...O.K.` + `VTref=3.3V`，但接着 `Failed to initialize DAP` / `Could not connect to the target device`。
- **辨析**：这里的 "DAP" 指**芯片内的 CoreSight 调试端口**，不是你的 DAP-Link 烧录器。探针通了、`VTref` 有电说明 **GND/VREF 通**，就差 **SWCLK/SWDIO 两根信号线**。
- **常见原因与解法（按概率）**：
  1. **SWD 排线松动 / SWCLK↔SWDIO 接反**——重插、换线（经常插拔的杜邦线内部断芯很常见，"之前能烧突然不行"多半是它）。
  2. **降速**——8MHz 对接触不良太快，`.jlink` 脚本里 `speed` 降到 4000 试。
  3. **固件复用了 PA13/PA14**——若代码把 SWDIO(PA13)/SWCLK(PA14) 配成普通 GPIO/其他复用，芯片跑起来会自锁调试口。本工程未这样做（只用了 PB12/PB13 给 CAN）。
  4. **板子断电重上电**——上次会话没干净释放，芯片 SWD 卡异常态。
- **注意**：`Cannot connect to the probe`（连不上探针，USB 层）和 `Failed to initialize DAP`（连不上芯片，SWD 层）是**两个不同阶段**的错，别混。

#### 坑6：J-Link 被 Ozone/Keil 独占

- **现象**：Ozone 或 Keil 开着调试会话时烧录，J-Link 连不上或报占用。
- **根因**：一个 J-Link 同一时刻只能被一个程序用。
- **解法**：烧录前关掉 Ozone / Keil UV4；若烧录异常中断，检查有无残留的 `JLink`/`Ozone`/`openocd` 进程占用：

  ```powershell
  Get-Process | ? { $_.ProcessName -match "JLink|Ozone|OpenOCD|UV4" }
  ```

#### 一句话总结

> **脚本只做到"插 J-Link 用 J-Link、否则用 DAP"，前提是电脑上一次只连一个烧录器。** 三条硬规则：① `.ps1` 保持纯 ASCII；② 检测用 `-PresentOnly` 不看 Status；③ 一次只连一个烧录器、且它必须接在板子上。

---

## 第12章 多工程共用一套快捷键（同一个键，按当前文件编烧对应工程）

把云台、底盘等多个独立工程放进同一套流程：**同一个 `Alt+, / Alt+N / Alt+M`，你在编辑哪个工程的文件，就编译烧录哪个工程**。无论"单独打开某个工程文件夹"还是"打开多根工作区"，都能用。

### 12.1 目录怎么放

多个工程**平级**放在上层目录，例如：

```
D:\Newcode\
├─ Gimbal_24hero_CH010_20260111\          ← 云台工程（自带 CMakeLists、Core、scripts 等）
├─ Chassisl_26_SHANGTAIJIE_SEND_DM\
│  └─ Chassisl_26_SHANGTAIJIE\            ← 底盘工程（结构同上）
└─ gimbal-chassis.code-workspace          ← 多根工作区文件（见 12.3）
```

每个工程都是**完整独立**的：各有自己的 `CMakeLists.txt`、`CMakePresets.json`、`cmake/`、`openocd.cfg`、`scripts/flash.ps1`、`.vscode/`。互不干扰，各自的 `build/` 也天然隔离（在各自目录下）。

### 12.2 核心原理：`${fileWorkspaceFolder}` 一招通吃

关键是把任务里的 `${workspaceFolder}` 换成 **`${fileWorkspaceFolder}`**（当前打开文件所属的工程根）。这个变量在两种打开方式下都正确：

| 打开方式 | `${fileWorkspaceFolder}` 解析为 | 快捷键效果 |
| --- | --- | --- |
| 单独打开云台文件夹 | 云台工程根 | 编烧云台 |
| 单独打开底盘文件夹 | 底盘工程根 | 编烧底盘 |
| 打开多根工作区 | 当前编辑文件所属的工程根 | 按当前文件分流 |

> **为什么不能用 `${workspaceFolder}`**：多根工作区里，VSCode 有个已知限制——用快捷键触发**同名** task（build/flash）时，它永远执行"第一个根"里那份，不按当前文件分流（microsoft/vscode #227350）。改用 `${fileWorkspaceFolder}` 后，即便 VSCode 选了第一个根的任务定义，命令实际仍指向当前文件的工程，从而正确分流。一份配置，单根/多根两种模式通吃。

### 12.3 新建多根工作区文件（逐字照抄）

在 `D:\Newcode\` 下新建 `gimbal-chassis.code-workspace`：

```json
{
    "folders": [
        {
            "name": "Gimbal (云台)",
            "path": "Gimbal_24hero_CH010_20260111"
        },
        {
            "name": "Chassis (底盘)",
            "path": "Chassisl_26_SHANGTAIJIE_SEND_DM/Chassisl_26_SHANGTAIJIE"
        }
    ],
    "settings": {
        "C_Cpp.errorSquiggles": "disabled"
    }
}
```

- `path` 相对于 `.code-workspace` 所在目录（这里是 `D:\Newcode`）。底盘路径含两层嵌套，逐字保留。
- **注意**：任务不放在这里（见 12.2 原因），只放 folders + settings。
- 用法：**双击这个文件用 VSCode 打开**（不再是"打开文件夹"），侧栏出现"Gimbal (云台)"和"Chassis (底盘)"两个根。

### 12.4 每个工程的 tasks.json（两个工程内容完全一致，逐字照抄）

每个工程的 `.vscode/tasks.json` 都写成下面这样——三任务全部用 `${fileWorkspaceFolder}`：

```json
{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "cmake --build --preset Debug",
            "options": {
                "cwd": "${fileWorkspaceFolder}"
            },
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": ["$gcc"]
        },
        {
            "label": "flash",
            "type": "shell",
            "command": "powershell -ExecutionPolicy Bypass -File \"${fileWorkspaceFolder}\\scripts\\flash.ps1\"",
            "problemMatcher": []
        },
        {
            "label": "build and flash",
            "dependsOrder": "sequence",
            "dependsOn": [
                "build",
                "flash"
            ],
            "problemMatcher": []
        }
    ]
}
```

- **build**：`options.cwd` 设为 `${fileWorkspaceFolder}`，这样 `cmake --preset Debug` 在当前工程目录里构建。
- **flash**：调各工程自己的 `scripts/flash.ps1`（第11章的自动切换脚本），路径用 `${fileWorkspaceFolder}`。反斜杠必须 `\\` 转义、内层引号 `\"` 转义（见 11.4 的坑）。
- 两个工程这份文件**内容一模一样**，直接复制即可。
- 前提：每个工程都要有自己的 `scripts/flash.ps1`（把第11章云台那份原样复制过去，因两工程产物名都是 `Gimbal_Demo`、都是 STM32F405RG，脚本无需改动即可通用）。

### 12.5 代码同步问题（重要澄清）

**单根编辑和多根编辑完全同步、双向同步。** 因为 `.code-workspace` 不复制任何代码，它只是"同时显示这几个文件夹"的视图配置。代码永远只有磁盘上那一份：

- 单独打开云台文件夹改了文件 → 多根工作区里看到的就是改后的。
- 多根工作区里改了文件 → 单独打开也是改后的。

没有"两份副本"，不会分叉、不会丢。就像用两个资源管理器窗口看同一个文件夹，改哪个都是同一个文件。

### 12.6 用法和验证

先**完全关闭 VSCode 再重开**（配置改动只对新窗口生效），然后三种模式都可用：

1. **单独打开云台文件夹** → `Alt+,` 编译、`Alt+N` 烧录、`Alt+M` 编译加烧录，作用于云台。
2. **单独打开底盘文件夹** → 同样的键，作用于底盘。
3. **双击打开 `gimbal-chassis.code-workspace`** → 在云台文件里按键编烧云台，切到底盘文件按键编烧底盘。

验证分流对不对：按 `Alt+,` 后看终端打印的构建路径，或 `[flash]` 那行的 ELF 路径，是云台目录还是底盘目录。

> 首次编译某工程若报 "ELF/HEX not found"，先按 `Alt+,` 编译一次生成产物再烧。

### 12.7 优势小结

- **一套快捷键，多个工程**：不用记"现在要编哪个"、不用切配置，按当前文件自动对号入座。
- **两种打开方式通吃**：单独开工程、开多根工作区都能用，靠的是 `${fileWorkspaceFolder}` 一个变量。
- **零副本、双向同步**：代码只有一份，怎么切都不会分叉或丢失。
- **各工程完全独立**：build 目录、配置、烧录脚本互不干扰；新工程按同样结构丢进 `D:\Newcode\` 并加进 `.code-workspace` 即可纳入同一套流程。
- **复用第11章成果**：烧录仍是 DAP/J-Link 自动切换，多工程照样享受。

### 12.8 本章新增 / 改动的文件小结

- **新增** `D:\Newcode\gimbal-chassis.code-workspace`：多根工作区文件（folders + settings，不含 tasks）。
- **改** 每个工程的 `.vscode/tasks.json`：三任务统一用 `${fileWorkspaceFolder}`（把原来的 `${workspaceFolder}` 换掉）。
- **新增** 底盘 `scripts/flash.ps1`：从云台原样复制（DAP/J-Link 自动切换，无需修改）。
- **未动** 两工程的 `CMakeLists.txt`、`CMakePresets.json`、`openocd.cfg`、`launch.json`、快捷键 keybindings、固件代码。

---

## 第13章 clangd 代码智能提示（补全 / 跳转 / 实时报错）

让 VSCode 写代码时聪明起来：自动补全、按住 Ctrl 跳转到定义、实时红波浪线报错、悬停看类型。这一层只负责"写代码体验"，不参与编译烧录（那是 gcc 的活），两者互不冲突。

### 13.1 clang / clangd / C-C++ 插件，别混

- **clang**：编译器，和 gcc 同类（把代码编成机器码）。本工程编译用 gcc，不用 clang。
- **clangd**：语言服务器，写代码时给补全/跳转/报错。名字带的 `d` 是后台服务的意思。
- **C/C++ 插件（微软）**：也能做提示（IntelliSense），还负责调试底层。

对标关系：**clangd ↔ 微软 C/C++ 的 IntelliSense**（二选一做提示）；**clang ↔ gcc**（编译器，本工程用 gcc）。

### 13.2 装 clangd + 关掉微软 IntelliSense（避免打架）

1. VSCode 扩展市场搜 `clangd`（LLVM 出品），安装。首次启用会提示下载 clangd 语言服务器本体，同意下载。
2. clangd 会弹窗提示"检测到与 Microsoft C++ 冲突"，点 **`Disable IntelliSense`**，它自动关掉微软的 IntelliSense（只关提示，不影响调试）。
3. **不要卸载**微软 C/C++ 插件——你的 cortex-debug 调试依赖它。只关它的 IntelliSense 即可。

> 结果：代码提示归 clangd（更准），调试仍归 C/C++ 插件，各司其职。

### 13.3 关键：让 clangd 找到 compile_commands.json

clangd 靠工程编译时生成的 `build/Debug/compile_commands.json` 才能精准解析（本工程 CMakeLists 已开 `CMAKE_EXPORT_COMPILE_COMMANDS`，编译一次就生成）。**前提：先按 `Alt+,` 编译过一次**，该文件才存在。

**坑（多根工作区）**：VSCode settings.json 里写 `"clangd.arguments": ["--compile-commands-dir=${workspaceFolder}/build/Debug"]`，在**单独打开工程时有效**，但在**多根工作区里只对第一个根生效**（clangd 已知限制，见 clangd/vscode-clangd #438/#923），导致别的工程满屏波浪线。

**正确解法：用 `.clangd` 文件**。在**每个工程根**（不是 `.vscode`）放一个 `.clangd`：

```yaml
CompileFlags:
  CompilationDatabase: build/Debug
```

`CompilationDatabase: build/Debug` 是相对该 `.clangd` 所在目录的路径。clangd 打开哪个工程的文件，就自动读那个工程的 `.clangd` → 找到对应的 compile_commands.json。**单独打开工程、多根工作区都正确，一劳永逸。**

同时把 settings.json 里的 `clangd.arguments`（如果加过）**删掉**，避免多根下干扰，配置全交给 `.clangd`。

### 13.4 用法和验证

1. 配好后，命令面板（`Ctrl+Shift+P`）运行 `clangd: Restart language server`，或重开 VSCode。
2. 等右下角 clangd 建立索引（首次稍慢）。
3. 验证：打开 `.c/.cpp`，输入 `结构体名.`（如 `PID_Yaw_mang.`）应弹出成员补全；头文件、类型的红波浪线应消失；Ctrl+点击函数能跳转。

> 波浪线不消：多半是没重启 clangd、还没编译过（无 compile_commands.json）、或 `.clangd` 放错位置（要在工程根）。

### 13.5 本章新增 / 改动的文件小结

- **新增** 每个工程根 `.clangd`：`CompilationDatabase: build/Debug`，让 clangd 找到编译数据库（单根/多根都生效）。
- **改** 每个工程 `.vscode/settings.json`：移除 `clangd.arguments`（改由 `.clangd` 接管），保留 `C_Cpp.errorSquiggles`（云台还保留 `files.associations`）。
- **装** clangd 扩展，并关掉微软 C/C++ 的 IntelliSense（保留其调试功能）。
- **未动** 编译烧录链（仍是 cmake+gcc+ninja / DAP-J-Link 自动切换）。

---

## 第14章 工程搬家 / 重命名后"全线报错"的急救（真实排障复盘）

这一章记录一次真实事故：把工程目录整个挪了位置（或改了名），结果"电脑好像炸了"——编译报错、快捷键失灵、代码满屏红波浪线、多根工作区侧栏空白。**其实工具链、环境变量、代码一个都没坏**，坏的只是那些"记死了绝对路径"的地方。搞清楚这一点，五分钟就能全部修好。

### 14.1 病根：三处记死了绝对路径

工程一旦换目录（重命名父文件夹、移动到别的盘符/路径），下面三处会同时失效，且报错各不相同，容易误判成"环境崩了"：

| 失效点 | 记死路径的文件 | 典型症状 |
| --- | --- | --- |
| **CMake 构建缓存** | `build/Debug/CMakeCache.txt` | 编译直接报错，见 14.2 |
| **多根工作区引用** | `xxx.code-workspace` 的 `folders[].path` | 打开工作区侧栏文件夹空白/失效，任务/快捷键找不到 |
| **clangd 索引缓存** | `%LOCALAPPDATA%\clangd\index`（后台库） | 代码满屏红波浪线，但其实能编过 |

> 关键认知：`build/` 里全是**可再生的编译产物**，CMake 缓存里存的是**绝对路径**。所以工程一搬家，缓存里记的老地址就对不上了。这也是为什么 `build/` 通常进 `.gitignore`——它本就不该跟着工程走。

### 14.2 症状一：编译报"源目录对不上"

按 `Alt+,` 编译，报类似：

```
CMake Error: The current CMakeCache.txt directory D:/Newcode/新路径/build/Debug/CMakeCache.txt
 is different than the directory D:/Newcode/旧路径/build/Debug where CMakeCache.txt was created.
...
CMake Error: The source directory "D:/Newcode/旧路径" does not appear to contain CMakeLists.txt.
ninja: error: rebuilding 'build.ninja': subcommand failed
```

**根因**：`build/Debug/CMakeCache.txt` 里记的还是搬家前的旧绝对路径（可用下面命令确认）：

```powershell
# 在工程根执行，看缓存里记的是不是旧路径
Select-String -Path build\Debug\CMakeCache.txt -Pattern "_SOURCE_DIR:STATIC|CMAKE_CACHEFILE_DIR"
```

**解法：删掉旧 `build` 目录，重新配置 + 编译**（删的全是产物，零代码丢失）：

```powershell
# 在工程根执行
Remove-Item -LiteralPath build -Recurse -Force   # 删旧缓存
cmake --preset Debug                              # 重新配置（重建 CMakeCache + compile_commands.json）
cmake --build --preset Debug                      # 重新编译
```

看到 `Linking CXX executable Gimbal_Demo.elf` 和内存占用表（`FLASH: xx%`）即成功。之后在 VSCode 里正常 `Alt+,` 即可。

### 14.3 症状二：多根工作区侧栏空白 / 任务找不到

双击打开 `xxx.code-workspace`，侧栏两个工程文件夹是空的或标红，快捷键也没反应。

**根因**：`.code-workspace` 里的 `folders[].path` 指向的还是旧路径。例如工程从 `Chassisl_26_SHANGTAIJIE_SEND_DM/Chassisl_26_SHANGTAIJIE` 挪成了 `Chassisl_26_SHANGTAIJIE`，或云台从 `Gimbal_24hero_CH010_20260111` 换成了 `Gimbal_26hero_CH010_20260725`，工作区文件却没跟着改。

**解法**：把 `folders[].path` 改成**当前真实存在**的目录名（相对 `.code-workspace` 所在目录）：

```json
{
    "folders": [
        {
            "name": "Gimbal (云台)",
            "path": "Gimbal_26hero_CH010_20260725"
        },
        {
            "name": "Chassis (底盘)",
            "path": "Chassisl_26_SHANGTAIJIE"
        }
    ],
    "settings": {
        "C_Cpp.errorSquiggles": "disabled"
    }
}
```

改完**完全关闭 VSCode 再重开**工作区（配置只对新窗口生效），侧栏两个根恢复正常。

> 快速自检：确认每个引用的路径都真实存在、且各工程都带齐 `.vscode/tasks.json`、`scripts/flash.ps1`、`.clangd`、`CMakePresets.json`（缺任何一个都会让"一套快捷键管多工程"失灵）。

### 14.4 症状三：代码满屏红波浪线（但能编过）

`MX_GPIO_Init()` 等 CubeMX 生成的函数下面全是红波浪线，鼠标悬停多半提示 `use of undeclared identifier` 之类，来源是 **clangd**（前提：你已按第13.2章关掉微软 IntelliSense，所以红线只可能来自 clangd）。

**根因**：clangd 还抱着搬家前的旧索引/旧 `compile_commands.json`，找不到函数声明。**代码和编译其实都没问题**（14.2 已重新编译成功）。

**解法（从轻到重）**：

1. `Ctrl+Shift+P` → `clangd: Restart language server`，等右下角重建索引，红线消失。
2. 还不消，关掉 VSCode 删 clangd 持久缓存再重开（缓存会自动重建，无副作用）：

   ```powershell
   Remove-Item -Recurse -Force "$env:LOCALAPPDATA\clangd\index" -ErrorAction SilentlyContinue
   ```

3. 前提是**编译过至少一次**（`build/Debug/compile_commands.json` 才存在，clangd 才能精准解析），且工程根有 `.clangd`（内容 `CompilationDatabase: build/Debug`，见第13.3章）。

### 14.5 一句话总结与预防

- **总结**：工程搬家后的"全线报错"不是环境炸了，而是三处**绝对路径缓存**失效——`build/` 的 CMake 缓存、`.code-workspace` 的 folders 路径、clangd 的旧索引。对症各修一处即可。
- **预防**：尽量别频繁重命名/移动工程根；真要搬，搬完记得 **① 删 `build/` 重新配置 ② 更新 `.code-workspace` 路径 ③ 重启 clangd**。`build/` 进 `.gitignore`，天然不跟工程走，换机/换路径都靠重新配置生成。

---

## 第15章 换个工程打开，快捷键 / 缩放 / 补全全没了（Profile 配置文件之坑）

又一次"电脑好像坏了"的真实事故：**同一个 VSCode，打开 A 工程一切正常（Alt+Q 查引用、Ctrl+滚轮缩放、clangd 补全都在），打开 B 工程却全部失灵**。代码、扩展、环境一个没坏，坏的是——**两个工程被关联到了不同的 VSCode "配置文件（Profile）"**。

### 15.1 病根：VSCode Profile（配置文件）机制

VSCode 有个"配置文件（Profiles）"功能：可以给不同文件夹关联**不同的一套**设置、快捷键、启用的扩展。一旦某个工程被关联到**默认（Default）Profile**，而你的全部自定义（Alt+Q / Alt+Z、`editor.mouseWheelZoom`、clangd 相关设置、甚至启用的扩展）都配在**另一个自定义 Profile**（本战队叫 `STM32`）里，那么：

| 工程关联的 Profile | 现象 |
| --- | --- |
| `STM32`（自定义，全部快捷键/设置都在这） | 一切正常 ✅ |
| `Default`（内置空配置） | Alt+Q/Alt+Z 失效、Ctrl+滚轮不缩放、clangd 补全消失 ❌ |

> 关键认知：Profile 是**跟"文件夹路径"绑定**的，不是跟工程内容绑定。所以复制/新建/搬家出来的工程，很容易落到 `Default` 上，而不是你惯用的那个自定义 Profile。

### 15.2 怎么确认是不是这个坑

1. **看左下角**：VSCode 窗口左下角齿轮 ⚙ 附近会显示当前 Profile 名。正常该是 `STM32`，若显示 `Default` 或别的名字，就是它。
2. **和好的工程对比**：打开那个一切正常的工程，看它左下角是什么 Profile 名（本战队是 `STM32`），两边不一致就实锤了。
3. 注意**不是**"受限模式（Restricted Mode）"——受限模式会在状态栏显示盾牌 🛡 图标并禁用扩展，那是另一回事（点盾牌选 Trust 即可）。Profile 坑没有盾牌。

### 15.3 解法：把当前工程切到正确的 Profile（30 秒）

在**出问题的那个工程窗口**里：

1. `Ctrl+Shift+P`
2. 输入并选 **`Profiles: Switch Profile`**（配置文件: 切换配置文件）
3. 选你惯用的那个（本战队选 **`STM32`**，别选 `Default`）

切完**立即生效**：快捷键、缩放、扩展全部回来。而且 VSCode 会**记住**"这个文件夹 → 用这个 Profile"，下次再打开自动用对。

> 底层原理（了解即可）：这个"文件夹→Profile"的关联存在 `%APPDATA%\Code\User\globalStorage\storage.json` 的 `profileAssociations` 段里，切换 Profile 时自动写入。手工改这个文件也能改关联，但**必须先完全退出 VSCode**（否则退出时会用内存里的旧值覆盖），远不如界面上 `Switch Profile` 一步到位。

### 15.4 附带坑：切到 STM32 Profile 后，Git Bash 终端 Ctrl+V 不能粘贴

切到 `STM32` Profile 后可能发现**终端里 `Ctrl+V` 粘贴不了**（而之前在 Default Profile 用的是 PowerShell 终端，能粘）。

- **根因**：`STM32` Profile 的默认终端是 **Git Bash**（`terminal.integrated.defaultProfile.windows: "Git Bash"`）。Git Bash 的 readline 把 `Ctrl+V` 解释成 "quoted-insert"（逐字插入下一个键），**不触发粘贴**。这不是 VSCode 的 bug，是 bash 的按键语义。
- **解法**：在 `STM32` Profile 的 keybindings.json（`Ctrl+Shift+P` → `Preferences: Open Keyboard Shortcuts (JSON)`）里加一条，让终端聚焦时 `Ctrl+V` 强制走 VSCode 的粘贴命令：

  ```json
  {
      "key": "ctrl+v",
      "command": "workbench.action.terminal.paste",
      "when": "terminalFocus"
  }
  ```

  保存即时生效（keybindings 热加载，不用重启）。若仍不行，说明当前终端标签是切 Profile 之前开的旧 bash，**关掉该终端标签重开一个新的**（`` Ctrl+` ``）再试。
- **备选**：不想动 keybinding，也可以直接在终端里用 **鼠标右键粘贴**，或把该 Profile 默认终端改回 PowerShell（`terminal.integrated.defaultProfile.windows` 改成 `"PowerShell"`）——但 Git Bash 对本工程的 `flash.ps1`/命令行更顺手，推荐保留 Git Bash + 上面那条 keybinding。

### 15.5 一句话总结

> 换工程后快捷键/缩放/补全全没，先看左下角 Profile 名——多半是落到了 `Default`，`Ctrl+Shift+P` → `Profiles: Switch Profile` → 选 `STM32` 即可全部恢复。切完若终端 `Ctrl+V` 不能粘贴，是 Git Bash 吞了该键，加一条 `terminalFocus` 下的 `workbench.action.terminal.paste` 绑定解决。

---

## 全文总结：这套配置到底做了什么

从一台空白电脑，到一套"多工程一键编译烧录 + 智能提示 + 双烧录器 + Ozone 调试"的现代 STM32 开发环境。各章成果与优势速览：

| 章 | 做了什么 | 核心优势 |
| --- | --- | --- |
| 1-2 | 装 gcc/CMake/Ninja/OpenOCD 四件套并配 PATH | 脱离 Keil，命令行工具链 |
| 3-4 | CMakeLists + 工具链文件（F405 参数、C/C++ 混编、优化/调试 flag） | 精确可控的构建；`.c` 里的 C++ 代码也能编 |
| 5 | openocd.cfg（CMSIS-DAP + SWD） | 免驱烧录 |
| 6-8 | tasks.json + 快捷键 `Alt+,` / `Alt+N` / `Alt+M` | 一键编译 / 烧录 / 编译加烧录 |
| 9-10 | 与 CubeMX 共存 + 避坑合集 | 重新生成代码不丢改动；常见报错速查 |
| 11 | flash.ps1 双烧录器自动切换 | 插 DAP 用 OpenOCD、插 J-Link 用原生工具，同一快捷键无感切换，还更快 |
| 12 | 多根工作区 + `${fileWorkspaceFolder}` | 多个工程共用一套快捷键，按当前文件编烧对应工程；单根/多根通吃 |
| 13 | clangd + `.clangd` 文件 | 补全/跳转/实时报错，多根工作区也生效 |
| 14 | 工程搬家/重命名后"全线报错"急救 | 删 build 重配 + 改工作区路径 + 重启 clangd，三处路径缓存对症修 |
| 15 | 换工程后快捷键/缩放/补全全没（Profile 之坑） | `Switch Profile` 切回 STM32；附 Git Bash 终端 Ctrl+V 粘贴修复 |

**整体优势**：
- **快**：Ninja 多核并行编译，增量一两秒；J-Link 原生烧录更快；Ozone 调试比 Keil 秒开。
- **省心**：一套快捷键管多个工程、两种烧录器；插上就用，不用切配置。
- **现代**：命令行工具链可脚本化、可进 CI；配 clangd 写代码体验一流。
- **兼容**：与 CubeMX、cortex-debug 调试、Ozone 全部共存；固件代码零侵入。

一句话：**编译烧录走成熟的 gcc + OpenOCD/J-Link，写代码体验走 clangd，多工程靠多根工作区统一，一套快捷键全搞定。**
